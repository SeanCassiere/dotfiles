return {
  "nvim-neo-tree/neo-tree.nvim",
  cmd = "Neotree",
  config = {
    filesystem = {
      filtered_items = {
        follow_current_file = true,
        visible = true,
      },
    },
  },
}
